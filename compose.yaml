services:
  food-is-life:
    build: .
    container_name: food-is-life
    ports:
      - "3000:3000"
    environment:
      PORT: "3000"
      DATA_DIR: /app/data
      STATIC_DIR: /app/dist
    volumes:
      - /mnt/user/appdata/food-is-life/data:/app/data
    restart: unless-stopped


